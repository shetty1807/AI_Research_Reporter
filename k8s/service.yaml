apiVersion: v1
kind: Service
metadata:
  name: ai-research-service
spec:
  type: NodePort
  selector:
    app: ai-research-reporter
  ports:
    - port: 8501
      targetPort: 8501
      nodePort: 30085